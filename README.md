	To Do List App (PHP, MySQL, Bootstrap, jQuery)

	Installation 
Import sql (to_do_list) to your database.
Database info in (db_conn) file.

	$hostname = "localhost";
	$user = "root";
	$pass = "";
	$db_name = "to_do_list";

